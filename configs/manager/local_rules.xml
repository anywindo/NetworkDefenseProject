<!-- Local rules -->

<!-- Modify it at your will. -->
<!-- Copyright (C) 2015, Wazuh Inc. -->

<!-- Example
-->
<group name="local,syslog,sshd,">

  <rule id="100001" level="5">
    <if_sid>5716</if_sid>
    <srcip>1.1.1.1</srcip>
    <description>sshd: authentication failed from IP 1.1.1.1.</description>
    <group>authentication_failed,pci_dss_10.2.4,pci_dss_10.2.5,</group>
  </rule>

</group>

<!--
<group name="snort,local,">

  <rule id="910001" level="5">
    <decoded_as>snort-full</decoded_as>
    <match>Nmap</match>
    <description>Snort: Nmap scan detected from $(src_ip)</description>
    <group>snort,scan,nmap,</group>
  </rule>

  <rule id="910002" level="8">
    <decoded_as>snort-full</decoded_as>
    <match>SSH</match>
    <description>Snort: SSH brute-force attempt from $(src_ip)</description>
    <group>snort,bruteforce,ssh,</group>
  </rule>

  <rule id="910003" level="10">
    <decoded_as>snort-full</decoded_as>
    <match>ICMP Flood</match>
    <description>Snort: ICMP flood or ping-based DoS from $(src_ip)</description>
    <group>snort,ddos,icmp,</group>
  </rule>

</group>
-
-->
